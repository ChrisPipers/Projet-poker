package model.observer;

/**
 * this interface allows to use Observer for the MVC
 *
 * @author g39864
 */
public interface Observer {

    /**
     * this method allows to call the method of the class used for update it
     */
    public void update();

}
